{% for block in section.blocks %}
   <div class="swiper-slide">
     <!-- Product Card -->
   </div>
{% endfor %}

{% schema %}
{
  "name": "main-slider",
  "settings": [
		{
			"type": "collection",
			"id": "collection",
			"label": "Collection"
		}
	],
  "blocks": [
    {
      "name": "Product",
      "type": "product",
      "settings": [
        {
          "type": "product",
          "id": "product",
          "label": "Product"
        }
      ]
    }
  ]
}
{% endschema %}